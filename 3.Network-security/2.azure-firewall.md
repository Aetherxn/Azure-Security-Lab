# Azure Firewall Deployment

## Objective

Deploy an Azure firewall to control inbound and outbound network access which is an important part of our overall network security posture.

---

## Azure Firewall Diagram

![Firewall Diagram](screenshots/firewall-diagram.png)

___

## Implementation (Security Controls)

- Deploy an Azure firewall.

![Firewall Deployment](screenshots/firewall.png)

- Create a default route.

![Default Route](screenshots/droute.png)

- Configure an application rule.

![Application Rules](screenshots/app-rule.png)

- Configure a network rule.

![Network Route](screenshots/net-rule.png)

- Configure DNS servers.

![DNS Servers](screenshots/dservers.png)

---

## Architecture Decisions

- Implement an Azure Firewall for intrusion protection/prevention, traffic control, and scalability.
- Creat a default route to direct unmatched traffic to the internet, firewall, or VPN.
- Add application rules to allow or block traffic to specific applications, websites, and domains.
- Add network rules to control traffic using IP addresses, ports, and protocols.
- Configure DNS Servers to translate domain name (e.g bing.com, microsoft.com) in IP addresses for connectivity.

---

## Validation

Test the firewall is working by connecting to the Srv-Jump virtual machine using Remote Desktop, then connect from Srv-Jump to Srv-Work. On Srv-Work, open Internet Explorer and confirm that bing.com loads successfully while microsoft.com is blocked with a deny message. This confirms the firewall rules are working correctly.

![Test Validation](screenshots/test-valid.png)

![Test Validation](screenshots/test-valid1.png)

![Test Validation](screenshots/test-valid2.png)

![Test Validation](screenshots/test-valid3.png)

![Test Validation](screenshots/test-valid4.png)

---

## Key Learnings

- I redeployed a new firewall as I misconfigured the first firewall which split azure resources and configurations between two separate firewalls causing shadowed firewall rule effect. Misconfigured firewalls can lead to data breaches, ransomware attacks or unexpected downtimes.
- **Firewalls** inspects and filters inbound and outbound traffic based on a defined set of security rules.
- **Routes** map or set of rules directing where network packets should travel across networks (e.g., from a local subnet to the internet or an Azure virtual network).
- **Application Rules** operate at Layer 7 that inspect the actual content, protocol, or fully qualified domain name (FQDN) of web traffic.
- **Network Rules** operate at Layers 3 and 4 (IP, port, and protocol level). They are used for non-web protocols or when traffic is best defined by an IP and port combination
- **DNS Servers** is the "phonebook" of the internet, resolving human-readable domain names to machine-readable IP addresses. 
